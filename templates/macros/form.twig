{% macro fieldset(formValue) %}
    {{ form_errors(formValue) }}
    <fieldset class="grix xs3 md8">
        {{ form_label(formValue,'',{'label_attr': {'class': 'self-left txt-left col-xs1 col-md3'}}) }}
        {{ form_widget(formValue, {'attr': {'class': 'form-control rounded-1 m-1 p-1 col-xs2 col-md5'}}) }}
    </fieldset>
{% endmacro %}
