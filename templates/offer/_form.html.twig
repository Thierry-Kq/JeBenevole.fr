{{ form_start(form) }}
<div>{{ form_row(form.title) }}</div>
<div>{{ form_row(form.address) }}</div>
<div>{{ form_row(form.zip) }}</div>
<div>{{ form_row(form.city) }}</div>
{#    <div>{{ form_row(form.longitude) }}</div>#}
{#    <div>{{ form_row(form.latitude) }}</div>#}
    <div>{{ form_row(form.isPublished) }}</div>
    <div>{{ form_row(form.isUrgent) }}</div>
<div>{{ form_row(form.description) }}</div>
    <div>{{ form_row(form.dateStart) }}</div>
    <div>{{ form_row(form.dateEnd) }}</div>
    <div>{{ form_row(form.file) }}</div>
    <div>{{ form_row(form.contactExternalName) }}</div>
    <div>{{ form_row(form.contactExternalEmail) }}</div>
    <div>{{ form_row(form.contactExternalTel) }}</div>
{# todo : if offer, choiceType on association #}
<button class="btn btn-primary">{{ button|default('form_btn_valider'|trans)}}</button>
{{ form_end(form) }}